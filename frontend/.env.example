# URL pública do backend (SchedNextAPI). Em dev, pode deixar vazio — cai em
# http://localhost:4000 por padrão (ver src/services/api.js). Em produção (Render),
# configurar como variável de ambiente do Static Site, ex: https://schednextapi.onrender.com
REACT_APP_API_URL=
